# 케이스 스터디 — 탄만큼

혼자 기획부터 배포까지 진행한 프로젝트다. 4개 역할(기획/개발·배포/콘텐츠/검증)을 전부 직접 맡았고, Claude Code와 Manyfast AI를 도구로 썼다 — 생성된 결과를 그대로 쓴 게 아니라 매번 검토하고 고친 부분을 아래에 남긴다.

## 문제

택시 합승 서비스는 보통 "목적지가 같은 사람"을 찾는다. 그런데 목적지가 정확히 같은 사람은 드물고, 그 조건 때문에 매칭이 잘 안 된다. 목적지가 달라도 "내 목적지가 상대방 택시 경로 위에 있으면" 같이 탈 수 있는 거 아닌가 — 여기서 시작했다.

## 기획: 매칭 기준을 경로로 다시 정의

저는 (목적지 일치 기준으로는 매칭될 사람이 거의 없을 상황에서) (Manyfast AI로 PRD·와이어프레임 초안을 여러 번 뽑아보며 "경로 포함 여부"를 매칭 기준의 핵심으로 좁히는 것)을 판단·수행했고, 그 결과 (출발지 근접 30·경로 적합 30·실제 경로 포함 25·시간차 10·인원 5점 배점의 매칭 규칙과 [PRD](PRD.md))가 남았다.

같이 확정한 것: 팟장의 최종 목적지를 지나쳐야 하는 사용자는 참가 불가, 한 사용자는 동시에 한 팟만 참여 가능. 자동 배정이 아니라 팟장이 직접 확정.

## 개발·배포: 경로 하나로 요금을 계산

저는 (하차지점을 경유지로 넣어 다시 길찾기를 호출했더니 택시가 지그재그로 돌고 직행 9,100원짜리 경로가 총액 14,000원까지 뛰던 상황에서) (택시는 출발지→팟장 목적지 경로 하나만 달리고, 각자 하차지점은 그 경로에 투영한 진행거리로 정렬해 구간 요금을 나누는 방식으로 바꾸는 것)을 판단·수행했고, 그 결과 ([tanmankeum.vercel.app](https://tanmankeum.vercel.app/) 배포와 `runSelfCheck()`로 검증하는 "팟 총액 == 직행 요금" 불변식)이 남았다.

카카오맵 SDK, 카카오모빌리티 길찾기 API, Supabase(Realtime 채팅)를 Claude Code로 구현하고 Vercel에 배포했다. REST API 키는 서버리스 함수(`api/kakao-route.js`) 뒤로 숨겨 클라이언트에 노출되지 않게 했다.

## 콘텐츠: 랜딩페이지로 반응을 모으다

저는 (프로토타입만 있고 잠재 사용자가 서비스를 이해할 방법이 없던 상황에서) (비슷한 합승 서비스(sametrip.app)의 구성을 참고해 랜딩페이지를 만들고, 이메일로 지역별 수요를 수집하는 것)을 판단·수행했고, 그 결과 ([랜딩페이지](https://tanmankeum.vercel.app/landing/)와 waitlist 신청 폼)이 남았다.

## 검증·QA: 우회 경로 버그 잡기

저는 (경로 밖 목적지를 가진 참여자가 항상 "출발지에서 하차"로 계산되던 버그에서) (`isRouteEligible`이 출발지에 대해 항상 `true`를 반환하던 원인을 콘솔에서 재현해 좁히고, 출발지는 목적지가 실제로 출발지일 때만 하차 후보로 남도록 고치는 것)을 판단·수행했고, 그 결과 (같은 경로로 4회 반복 호출해도 거리·요금·시간이 동일하게 나오는 회귀 시험과 [QA 노트](qa-notes.md))가 남았다.

경로 우선순위를 `RECOMMEND`에서 `DISTANCE`로 고정한 것도 같은 회차에서 나온 결정이다 — 실시간 교통 반영 때문에 같은 팟이 접속 시점마다 참가 가능/불가로 뒤집히는 문제였다.

## 남은 것

`dissolvePod`(팟장 취소 시 재매칭 복귀)의 동시성 경계 케이스, 길찾기 API 장애 시 폴백 경로는 코드는 있지만 실제 장애를 재현해 확인하지 못했다.
