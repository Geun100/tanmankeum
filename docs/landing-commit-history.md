# 랜딩페이지 커밋 기록 (백업용)

생성일: 2026-08-25 · `git log --follow -- landing/` 기준, 오래된 순

---

## 89a0d0a — 랜딩 자료를 landing/ 폴더로 분리
앱(index.html, kakao/supabase 키, api/)과 랜딩이 둘 다 루트에 흩어져 있어 편집할 때 섞이기 쉬웠음.
랜딩 페이지, waitlist SQL, Supabase 키 파일을 전부 `landing/` 아래로 옮김. 루트 앱은 손 안 댐.

## 9e656d8 — 랜딩 문구가 실제 프로토타입과 다른 부분 수정
"실제 앱 화면" 목업과 문구를 index.html 실제 동작과 대조:
- "매칭 92점", "약 71% 절약" 배지 제거 — 실제 앱은 매칭점수나 절약 퍼센트를 사용자에게 안 보여줌. "8,500원 절약" 식(실제 앱 스타일)으로 교체.
- "팟장 최종 목적지" → "최종 목적지" — 실제 지도 범례 라벨과 일치시킴(트렁크 목적지가 팟장 본인 목적지가 아닐 수 있음).
- FAQ "목적지가 팟장보다 멀면?" 답변 재작성 — 기존 답변은 실제 매칭 로직과 반대로 써있었음.

## 0e5f81f — 손으로 그린 히어로 목업을 실제 앱 마크업/CSS로 교체
실제 프로덕션(tanmankeum.vercel.app)을 홈 화면 상태로 조작해 실제 DOM(outerHTML)과 CSS를
그대로 가져옴. 손으로 만든 근사 목업이 아니라 진짜 컴포넌트.

## 6a33379 — /landing 이메일가입 404 수정, 한동대/지방대생 타겟으로 문구 전환
- `waitlist-keys.local.js`가 상대경로라 트레일링 슬래시 없는 `/landing` 접속 시 루트로 잘못 해석돼
  항상 "등록 실패"였음. 절대경로로 고침.
- 문구를 포항 중심에서 한동대/지방대생 중심으로 전환(히어로, 칩, 푸터, FAQ). "왜 탄만큼" 섹션을
  기존 합승 서비스의 실제 불편 3가지(동승자 찾기 어려움/목적지 달라야만 가능/1/N 균등분배) 기준으로 재작성.

## a0bf39a — 이용방법 4개 카드를 실제 앱으로 연결
정적 div였던 카드를 실제 서비스 시작점(1단계 이동정보 입력)으로 연결.

## 11f26f7 — "이용 방법" 섹션 재설계: 클릭 시 화면 안에서 미리보기
기존: 카드 4개가 전부 "/"(루트 앱)로 나가는 링크라, 누르면 랜딩을 완전히 벗어남.
변경: 왼쪽 4개 스텝 클릭 시 오른쪽 폰 목업 하나가 내용만 바뀜(페이지 이동 없음).

## 57d5430 — 손으로 재현한 목업을 실제 앱 스크린샷으로 교체
`landing/scripts/seed_demo_pods.py` + `shoot.py`: Playwright로 실제 온보딩→홈→상세→채팅
플로우를 프로덕션에서 직접 조작해 캡처. 목업이 아니라 실제 DB 행을 실제 앱이 렌더한 화면.
캡처용 계정/팟은 촬영 후 Supabase에서 삭제.

## dfae3ec — 히어로 폰 스크린샷을 잘라서 페이드하던 것 제거
`.shot--hero`가 max-height:520px + overflow:hidden + 하단 페이드로 실제 스크린샷의 위쪽
75%만 보여줘서 폰 하단 라운드/홈 인디케이터가 없어 비율이 이상했음. 자르기 제거, 대신 히어로
폰 자체를 다른 `.shot`보다 좁게(250~270px) 잡아 전체 화면을 다 보여주면서도 헤드라인 안 밀리게.

## be4ff9e — 모바일에서 과대 표시되던 폰 목업 축소, 오래된 채팅 스크린샷 갱신
실제 폰(375~430px)에서 `.shot` 이미지가 최대 300~330px까지 차지해 화면을 거의 다 잡아먹음 →
640px 이하에서 `.shot` 220px, `.shot--hero` 190px로 추가 축소.
`05-chat.png`가 왕관배지/문구추천삭제/팟정보 도착시각 추가 이전 버전이라 실제 앱과 달랐음 →
프로덕션에서 Playwright로 재캡처.

## 81651da — 실제 아이폰14 사이즈로 스크린샷 전체 재촬영
기존은 430x880 전체페이지 캡처라 실기기 비율(0.462)이 아니었음. 아이폰14 논리해상도
390x844 뷰포트 기준(전체페이지 아님)으로 5장 전부 재촬영. `shoot.py` 컨텍스트도 맞춰 수정,
`<img>` width/height 속성도 실제 비율로 교정.

---

### 참고
- 스크린샷 재촬영 스크립트: `landing/scripts/shoot.py` (UI 바뀌면 재실행)
- 이메일 수집 DB: 앱 DB와 별도인 Supabase 프로젝트(`landing/waitlist-keys.local.js`, `.gitignore`됨 — 커밋 기록에는 값 없음)
